# - Centralised application settings.
# - Reads environment variables from the .env file
# - Exposes a single settings object imported by other modules to avoid
#   repeated env calls across different files.
