# Server
HOST=0.0.0.0
PORT=8000

# Database (SQLite file stored in backend folder)
DATABASE_URL=sqlite:///./backend/ecu_data.db

# TLS
TLS_CERT_PATH=
TLS_KEY_PATH=

# CORS, comma separated list of allowed frontend origins
ALLOWED_ORIGINS=http://localhost:5173
